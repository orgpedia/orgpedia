images_dir: /Users/mukund/orgpedia/rajpol/import/images
ignore_docs:
  R.P.STransfer02042013_125038.pdf: This document has two tables on a single page
pipeline:
  - name: infer_headers
    config:
      conf_stub: inferheaders

  - name: pdfpost_parser
    config:
      conf_stub: pdfpostparser

  - name: pdforder_builder
    config:
      conf_stub: pdforder

  - name: id_assigner_fields
    config:
      conf_stub: 'id_assigner'
      post_id_fields: ['dept', 'role', 'juri', 'stat']
      cadre_file_dict:
        'I.P.S.': input/ips_officerIDs.json
        'R.P.S.': input/rps_officerIDs.json

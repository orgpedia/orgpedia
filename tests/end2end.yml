images_dir: /Users/mukund/orgpedia/rajpol/import/images
ignore_docs: [ ]
pipeline:
  - name: pdf_reader
    config:
      x_tol: 1
    
  - name: pdftable_finder
    config:
      edit_doc: True
      pdfplumber_config:
        vertical_strategy: lines
        horizontal_strategy: lines
        
  - name: infer_headers
    config:
      conf_stub: inferheaders

  - name: pdfpost_parser
    config:
      conf_stub: pdfpostparser

  - name: pdforder_builder
    config:
      conf_stub: pdforder

  - name: id_assigner_fields
    config:
      conf_stub: 'id_assigner'
      post_id_fields: ['dept', 'role', 'juri', 'stat']
      cadre_file_dict:
        'I.P.S.': ips_officerIDs.json
        'R.P.S.': rps_officerIDs.json
      
        
    

# R.P.STransfer03052017_125052.pdf: YML error
